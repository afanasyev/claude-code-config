## Restrictions

Do NOT read or reference `.env` files. Environment variables are managed separately and access is blocked by a hook.

## Agent usage

Use agents in parallel when it would improve the result. Check available agents and skills, use existing ones when they fit, fall back to general-purpose otherwise.
